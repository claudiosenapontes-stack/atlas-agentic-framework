# 2026-02-05

## Ops / Moltbot (Clawdbot/OpenClaw) health check
- Verified plugins: Telegram + WhatsApp loaded/enabled; other channel plugins installed but disabled (normal).
- Verified channels via `clawdbot channels status --probe`: all Telegram bots working (henry/einstein/harvey/severino/sophia); WhatsApp linked/connected with allowlist.
- Verified hooks: 3/4 ready; missing hook `soul-evil` is non-essential.
- Noted earlier: gateway latency spikes (~1–2.6s) can contribute to perceived slowness.
- Henry main session context was large (~249k/400k tokens) → recommend /new to improve responsiveness.

## Google / gog API health
- Distinction: Gmail Watch hook (Pub/Sub → OpenClaw hook) is working; `gog` CLI operations were failing in non-interactive env due to keyring password/TTY issues.
- Attempted fix: set `GOG_KEYRING_PASSWORD` via gateway config + generated/stored a random password at `/root/.openclaw/credentials/gog_keyring_password`.
- Test `gog auth list` failed with `aes.KeyUnwrap(): integrity check failed`, indicating token encryption password mismatch (new password can’t decrypt existing token store).
- Reverted `GOG_KEYRING_PASSWORD` back to empty in gateway config to avoid behavior changes.
- Next fix options: provide original keyring password OR re-authenticate gog with a known password (requires interactive/TTY or token store migration).

## Security note (outstanding)
- `/root/.clawdbot` is a symlink to `/root/.openclaw`. Prior audit flagged world-writable perms (777) on state dir; recommended to set to 700 (needs explicit approval). 
